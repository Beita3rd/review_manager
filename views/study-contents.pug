extends layout

block content
  - var hasContents = studyContent.length > 0
  if hasContents
    h3 勉強内容の一覧
    each content in studyContent
      p #{content.study_contents}： #{content.created_at}

    a(href="/study/new") 勉強内容の保存
          